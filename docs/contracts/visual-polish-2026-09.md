# 2026-09 战场视觉接线（向后兼容）

本轮在当前任务完成 04 美术表现及 03/08 客户端接线；不改共享规则、战斗事件或服务端。

- GPT 网页「雷鑫」制作正式位图，Blender 5.2 保留地形验证和最终材质源文件；运行时仍为 Phaser 2D，保留 tile-road / tile-grass / tile-deployment 稳定键。源文件位于 art_sources/。
- 地图尺寸、格子坐标、镜像、可布阵/可铲地语义不变；连续道路仅取消内部重描边，保留地形交界。
- 战况 HUD 在原有 200px 顶部区域排布，不能和兵营、本局 BUFF、征兵按钮重叠。
- 大招依赖已有 general-skill 事件，采用来源/目标局部表现，技能名放在顶部战况区；不改变伤害、范围、时序或联网数据。
- 动效遵循 reduced-motion；不增加循环粒子或实时 3D。资源按既有静态缓存加载，不随房间快照传输。
- 验收：新贴图来源/尺寸/体积核验；桌面、手机布局，布阵/合成、BUFF 翻页/拖放和绝技回归。测试账号隔离，不操作真实玩家房间。
- 性能预算：客户端 JS 上限由 1,665,000 调整为 1,680,000 bytes（小于 0.91%），CSS 31,000 bytes；新增完整美术的活动光栅总量仍须低于原来的 1,843,376 bytes。运行时代码只包含资源键，不携带制作元数据；普通攻击特效按批次和同时存活数量限流，技能/BUFF 事件不因普通攻击批次挤占而丢失。
- 新增稳定资源键 `buff-{invulnerable,haste,giant,rally,smoke,decoy}` 与 `fx-art-{thrust,crescent,shockwave,impact}`。前者由 DOM 图标及说明面板使用，后者由 Phaser 本地加载；对外战斗命令、快照、服务端事件不变。
