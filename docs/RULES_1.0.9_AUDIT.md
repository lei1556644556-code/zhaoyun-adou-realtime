# 《赵云与阿斗》1.0.9 规则与数值证据审计

审计日期：2026-09-03；负责模块：01 规则与数值基线。

机器配置：`packages/shared/src/config.ts` → `RULES_CONFIG_1_0_9`；规则版本：`1.0.9`；配置结构版本：`1.2.0`。

## 1. 审计结论

现仓库除候选取证稿外，已保存一份不含完整业务源码和美术的最小控制流/常量摘录，能够复核普通兵与武将的独立成长曲线、攻击形式和索敌、`d.Si` 命中几何、敌军速度、Boss 类型/血量、主要武将技能计数，以及包子、御敌千里、农民、行军丹和稀有度显示。原 XAPK、完整解包目录、DCC 清单与实机录像仍不提交，因此未进入摘录的细节不能据此宣称已完整重放。

因此本审计使用三种状态：

- `package-recorded`：候选取证稿或最小恢复代码摘录支持该项，且当前 Markdown/TypeScript 与之相符；每项可复核范围以证据引用为准。
- `pending-original-verification`：只有二手整理、存在冲突，或缺少足以确认精确算法/触发条件的原包片段。
- `project-adaptation`：原作广告/分享改为直接获得、10Hz 权威推进等本项目新增决策，不得标成原包规则。

发布冻结要求比 `package-recorded` 更高：随机消费顺序、逐帧轨迹、Boss/道具完整调用链仍需固定输入输出夹具复算。

**正式发布判定：目前仍不得宣称“与原版 1.0.9 完全一致”。** 已核对部分可以表述为“按恢复代码冻结”，但 Boss 12 套技能、黄忠箭雨逐帧空间结算、地图逐帧移动、推土车距离与摸金奖励链仍有证据缺口。每日前三局的铲子加权条件已经由 `on.UO` 恢复代码闭合，不再属于缺口。发布说明只能精确列出已冻结域和项目适配，不能使用“原包完全一致”“100% 复刻”等表述。

仓内一致性已经由配置/模拟测试锁定的域包括：开局与棋盘常量、基础牌池及每日前三局 `108→110` 分母、整局姓名字消耗牌库、招贤榜逐份复制、普通兵与武将独立成长曲线、攻击形式与索敌、擦边命中、20 波与 3 条难度曲线、`5:2:3` 难度权重、新手生命系数、普通/Boss 速度、Boss 类型轮换与 7/10/14 倍血量、四张地图格子矩阵、25 行道具目录及部分精确效果。横向自动合将、竖向不合将、移开拆将和左右候选确定性也有命令级回归测试；它只证明当前产品规则实现正确，不自动提升原包证据等级。

## 2. 证据库存

| 证据 | 仓库位置/标识 | 可证明内容 | 局限 |
|---|---|---|---|
| 规则候选稿 | 根目录 `赵云与阿斗_真人实时对战完整规则案_v0.9候选稿.docx`；SHA-256 `5417a0ed518196ce59bc1c3ddf23f06ef5383f2014652fed3eaaf735efa57e84` | 表 10–24 记录牌池、兵种、武将、成长、地图、波次、难度、Boss、道具原表；附录 D 记录取证过程 | 文档自称“候选稿，不是冻结稿”；不是原包本身，且正文有内部矛盾 |
| 原包身份记录 | Android 1.0.9 (10)，`com.zyyad.mihuan`；记录的 XAPK SHA-256 `a7c89bb6aab69910d510164361c37127130f7126c8200a39fd0bf8a0667336bd` | 指明目标版本 | XAPK 未随库，无法重新验哈希或解包 |
| 最小恢复方法摘录 | `docs/evidence/ORIGINAL_1.0.9_RULE_METHODS.md` | `Et`、`d.Si`、波次/Boss、武将计数器和部分道具类的控制流与常量 | 不含完整原包，尚未覆盖全部 Boss/道具调用链与逐帧表现 |
| 规则说明 | `docs/RULES_1.0.9_BASELINE.md` | 当前目标规则的可读说明 | 部分精确算法只有结论，没有原包定位片段 |
| 道具说明 | `docs/PROPS_1.0.9_IMPLEMENTATION.md` | 25 道具的当前解释、获得情景与网页映射 | `ja`/`Ha` 语义与候选稿冲突；精确方法片段未随库 |
| 版本化配置 | `packages/shared/src/config.ts` | 客户端/服务端可共享的机器值、结构化道具效果、证据状态 | 部分消费者尚未迁移，仍有硬编码副本 |
| DCC 工具 | `tools/extract-laya-dcc.cjs` | 可索引并恢复 Laya DCC 2.0 缓存，含路径逃逸保护 | 没有输入、输出清单、运行日志或校验报告 |

DOCX 的视觉渲染在当前环境因缺少 LibreOffice/`soffice` 未完成；本次只做了完整段落、表格与 OOXML 结构化读取。此限制不影响数值比对，但不能把它当成文档版式验收。

## 3. 已由原包取证记录支持的项目

以下项目状态为 `package-recorded`，证据定位同时写入 `RULE_PROVENANCE`：

| 域 | 当前收口值 | 记录定位 |
|---|---|---|
| 开局/棋盘 | 8×10、80px 格、阿斗 3、馒头 20、首招 10、每次 +2、准备/同波出兵/波间为 10/1.5/5s | 候选稿 2.1、表 6、附录 timing；基线 §1 |
| 征兵牌库 | 23 条目，总权重 **108**；每日前3局铲子由11增至13、总量110；基础兵/铲子保留，姓名字抽中后移除；招贤榜逐份姓名字50%复制 | 恢复代码 `on.startGame/UO/FO/TE`；基线 §2 |
| 基础兵种 | 攻击、0.8s 间隔、1.5/3.5/2.5/2 格射程、攻击形态与索敌 | 候选稿表 11；基线 §4 |
| 武将 | 12 组姓名、攻击/间隔/射程/上限、技能文字 | 候选稿表 12、15；基线 §3、§5 |
| 等级倍率 | 普通兵攻击/攻速均为 `1/1.5/2.1/2.73/3.4125`；武将攻击 `1/1.5/2.1/2.73/3.276`，攻速 `1/1.3/1.56/1.794/1.9734` | 恢复代码 `Et`；基线 §4–5 |
| 命中与兵种攻击 | 半径减 1px 后与敌兵整格含边界相交；枪为 15×71px 刺击；骑为半射程/全射程各 50% 环扫 | 恢复代码 `d.Si` 与普通兵攻击类；基线 §4 |
| 波次与难度 | 20 波数量/基础生命；3 条曲线及 5:2:3 权重 | 候选稿表 21、22；基线 §6 |
| 敌军与 Boss | 普通/Boss 速度 50/10px/s；里程碑 3/6/9/12/15/18 波，概率 10/20/30/50/90/100%；每图三 Boss 循环，生命 7/10/14× | 恢复代码 `S`、`It.Ty`、`It.Ry`；基线 §6 |
| 新手生命修正 | 账号前 10 局且仅前 10 波，按账号局数使用 `.6/.6/.6/.6/.7/.7/.7/.8/.8/.8` | 恢复代码 `S.ee`；基线 §6 |
| 地图矩阵 | 巨鹿、云梦泽、虎牢关、赤壁四张 8×10 `[x][y]` 表 | 候选稿表 16–19；基线 §7 |
| 道具原始目录 | 25 行 ID、名称、原始稀有度数值、价格、冷却、`ja`、`Ha` | 候选稿表 24；道具文档 §2 |
| 道具槽 | 主动 2、被动 6；当前 ID 分组 | 候选稿 §6.1、附录字段表；道具文档 §1 |
| 已核精确道具 | 包子 10 次、55/45 且不限初始上限；御敌千里只接受弓兵/武将；农民 Lv1–5 为 20/10/5/3/2 秒；招贤榜逐份50%复制；行军丹 +10；稀有度名称/色值 | 恢复代码 `Bh`、`Dh.NP`、`_h.BP`、`on.TE/FO`、购买动画与 `F.rn`；道具文档 §2 |

“支持”仅表示候选取证记录与当前配置一致。把这些项提升到“仓库可复核冻结”仍需第 6 节的最小证据包。

## 4. 仍待原包验证

| 域 | 当前记录/实现 | 缺口与冻结条件 |
|---|---|---|
| 营地回收 | 招募前回收；等级价值 `2^(level-1)`；铲子 1 | 缺少原方法与 Lv1–5 固定样例 |
| 两格武将/拆字 | 产品确认战场横向相邻立即合将，移走任一字立即失效；从两格中心算射程 | 候选稿描述的是经验升级体系，没有提供这套交互的原包定位；当前按产品确认实现，仍需实机录像或方法摘录补原包证据 |
| 地图路线插值 | 当前 `path` 折点与线性插值会始终落在候选稿记录的道路格内 | 地图格子矩阵为 `package-recorded`；精确折点与插值属于 `project-adaptation`，缺少原包移动方法或同帧轨迹对照 |
| 推土车 | 敌人向出生端推回 | 推动距离、碰撞/多目标/消耗条件未知；配置明确以 `null` 保留 |
| 练兵符 | Lv1–2 必胜；Lv3 70/30；Lv4+ 60/40 | 候选表只写“概率升降”；需方法分支摘录或足量固定随机样例 |
| 砚台/陷阱/地雷 | 半径 1.5、-20%/5s；眩晕 5s；爆心 60px 直接消灭 | 候选表只有概述；需精确常量与触发方法 |
| 降妖符 | Boss 施法 50% 失败并反噬 | 反噬数值/公式未知；没有实际 Boss 施法事件证据 |
| 农民/洛阳铲 | 30s 生农民、Lv1–5 每20/10/5/3/2s产1；60s生铲子 | 农民升级生产间隔已核；出生位置优先级与满营地行为仍缺完整调用链 |
| 陨石 | 300s 冷却、最后 6 路格、每格 1–2 颗、半径 1 格直接消灭 | 候选表仅写“接近阿斗”；需触发阈值与落点算法 |
| 摸金校尉 | 当前记录称只改金铲外观、无奖励 | 与“铲出宝箱”文案冲突；需开垦事件调用链 |
| `ja`/`Ha` | 当前网页把两字段解释为八格候选/最终中奖权重 | 候选稿明确说业务命名待核；需商店/转盘调用链，验证无放回算法和升级读取顺序 |
| 原作获得情景 | 局内铲子补给、胜 20/败 5、广告翻倍、战后 3 商品、10% 广告商品、八格转盘、日清空 | 目前只有实现说明；需原 UI 触发条件、配置或实机步骤。网页“直接获得”本身是已批准 `project-adaptation` |
| Boss 技能 | 12 个类型的范围、间隔、说明已记录；生命、类型轮换和速度已接入模拟 | 12 套施法目标、状态解除、召唤/转化等完整效果尚未实现和固定输入复算 |
| 黄忠箭雨 | 已按触发阈值与单箭 2 倍接入确定性模拟 | 当前模型尚未逐帧复现每支火箭的落点与碰撞，仍是规则等效而非空间逐帧等价 |

## 5. 已发现并处理的冲突

| 冲突 | 判定 |
|---|---|
| 基线曾写基础总权重 109、加权后 111 | 23 项实际求和及候选表均为 108；已改为 108，加 2 后为 110 |
| 候选稿 §3.1 正文写末级倍率 3.4125，表 14 写 3.276/1.9734 | 恢复代码证明两者属于不同单位曲线：普通兵用 3.4125，武将攻击/攻速用 3.276/1.9734；已拆成四条配置 |
| 稀有度名称存在“普通/稀有/卓越/史诗”与“稀有/卓越/史诗/传说”两种记录 | 恢复代码 `F.rn` 证明显示名称为后者，并同时冻结四色值 |
| 招贤榜“武将概率 ×2”与“每份姓名字 50% 追加一份” | 恢复代码 `on.TE/FO` 支持后者，并证明姓名字来自整局消耗牌库；已按控制流实现 |
| 御敌千里“远程单位”与旧实现“任意己方单位” | 恢复方法 `Dh.NP` 证明仅弓兵或已合成武将，现实现已拒绝刀/枪/骑普通兵 |
| 行军丹文案 +10 与旧记录“运行值 +1” | 原购买动画循环十次单点增长，合计 +10；现配置和账号逻辑统一为 +10 |
| `ja`/`Ha` 已反向核对与候选稿“业务语义待核” | 保留原始数值；当前网页映射继续作为兼容行为，但证据状态降为待核验 |
| 实时服务器 10Hz 与候选稿 `server_tick_hz=RT_TBD` | 标为项目适配，不列入 1.0.9 原作规则 |

## 6. 版本化配置与消费者迁移

`RULES_CONFIG_1_0_9` 已把下列域统一挂到一个公开入口：

- `recruitment`：牌池、权重合计、早期账号加权、营地容量与回收规则；
- `merge`：同兵种升级与 12 组武将字关系；
- `attackRange`、`units`：命中几何、攻击/攻速/射程和等级倍率；
- `props.catalog/effects/acquisition`：原始道具表、结构化效果、获得情景与广告直接领取映射；
- `waves`、`maps`：波次、难度、Boss 概率和地图；
- `evidence/provenance`：来源、状态与证据定位。

`packages/shared/test/rules-config.test.ts` 对完整地图、完整道具目录和完整武将配置使用确定性序列化指纹防漂移锁，并对练兵符、包子、升职令、Boss、难度和广告商品等结构化概率做取值范围或概率和校验。该指纹只证明仓内冻结配置没有静默漂移，不是原 XAPK 或其提取产物的证据摘要。

为保持已有接口兼容，本轮没有删除 `GAME_CONFIG`、`TOKEN_POOL`、`SOLDIERS`、`GENERALS`、`PROPS` 等旧具名导出。以下跨模块接线需由对应任务完成：

1. 模块 02：继续把尚未冻结的 Boss 技能和剩余道具调用链结构化；当前不得用文案猜测缺失字段。
2. 模块 03：界面只能显示结构化配置和明确证据状态，不得把 `null` 待核字段解释成确定值。
3. 模块 05：继续由 `props.acquisition` 驱动战后金币、商品、广告直接领取替代、转盘与日重置。
4. 模块 00/08：快照已记录 `rulesetVersion` 与 `rulesConfigSchemaVersion`；未来改变冻结值必须同步升级并提供迁移。

本轮权威模拟已迁移到独立兵/将曲线、新手生命系数、敌军/Boss 速度与血量、Boss 类型轮换、农民等级产出、包子次数、御敌千里目标资格和主要武将技能计数。联网兼容层增加心跳、命令确认超时和失败不清档；正式独立权威服务迁移见 `docs/contracts/realtime-authority-v2.md`。

## 7. 最小证据补齐清单

不需要提交完整原包或美术资源。建议由合法持有者生成以下最小证据后再冻结：

1. 原 XAPK 的版本、包名、文件大小和 SHA-256 校验输出；
2. DCC 树清单（路径、对象 ID、SHA-1/大小）及 `extract-laya-dcc.cjs` 运行日志；
3. 仅包含相关配置行的脱敏 JSON/文本摘录：牌池、兵种、武将、波次、地图、道具；
4. 相关方法的最小控制流摘录或伪代码：`d.Si`、招贤榜、练兵符、补给触发、`ja`/`Ha` 调用链、陨石、行军丹、摸金校尉；
5. 对每个随机规则给出固定输入、随机值、预期输出，不只给统计结论；
6. 对广告获得情景给出逐步实机证据，明确首次、后续、满营地、跨日与刷新行为；
7. 生成证据清单哈希并由规则模块测试校验，证据变更必须提升配置结构版本或规则版本。

在这些材料到位前，`pending-original-verification` 项可以作为当前兼容实现继续开发，但不得在发布说明中宣称为 1.0.9 已验证冻结。
