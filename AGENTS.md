# 赵云与阿斗项目协作规范

本仓库按正式游戏项目拆分为独立模块。每个 Codex 任务只拥有一个模块；跨模块协作必须经过稳定接口，不得复制规则数值或直接穿透修改其他模块内部实现。

## 权威来源

- 原版 `1.0.9` 的规则、数值、概率、攻击范围、道具获得条件以 `docs/RULES_1.0.9_BASELINE.md` 和规则模块的可追溯配置为唯一依据。
- `packages/shared` 是客户端、服务端和测试共同使用的类型、配置、战斗命令、快照和确定性模拟层。
- 服务端是实时对战的最终裁决者；客户端只提交命令并展示服务端快照与事件。
- 不确定的原版规则不得自行补数值。先记录证据缺口，并由“规则与数值基线”任务确认。

## 模块所有权

详细边界见 `docs/PROJECT_MODULES.md`。任务默认只修改自己拥有的目录和文件。确需跨模块修改时：

1. 先在 `docs/contracts/` 新建或更新接口说明；
2. 保持现有接口向后兼容，或者给出明确迁移方案；
3. 将实际接线交给“集成发布与线上运维”任务；
4. 不在多个模块中保存同一套规则、概率或经济数值。

## 交付要求

- 修改前先检查工作树，保留用户已有改动。
- 业务逻辑必须有自动化测试；界面与交互需要桌面端和手机端验收。
- 每次交付说明：改动文件、接口变化、验证命令、已知风险和待集成项。
- 非发布模块不得部署生产、修改 GitHub Pages 工作流或变更线上密钥。

